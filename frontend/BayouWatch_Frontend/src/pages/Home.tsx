import React from "react";

const Home: React.FC = () => {
    return (
        <main style={{ padding: "2rem", fontFamily: "Segoe UI, Roboto, -apple-system, sans-serif" }}>
            <h1>Home Page (Main Page)</h1>
            <p>This is a placeholder Home page for the BayouWatch frontend.</p>
            <p>Replace this content following the Figma design. Currently, all pages have a navbar + 'Go to Home' button</p>
            <div style={{ marginTop: "1rem" }}>

            </div>
        </main>
    );
};

export default Home;